# ============================================================
# EVA MODULE CONTRACT: PRN (Prompt Rule Node) Payload
# Version: 9.6.2-V2 (Strict)
# Status: LOCKED
# ============================================================

module: "PRN"
bus_channel: "bus:knowledge"
schema_ref: "file:///e:/The%20Human%20Algorithm/T2/agent/orchestrator/Module/CIM/Node/prompt_rule/schema/PRN_Payload_Schema_v2.json"

payload_structure:
  behavior_policy:
    type: "Array of Strings"
    description: "Active directives that govern LLM response behavior."
  
  persona_data:
    type: "Object"
    description: "Identity anchors and core persona attributes."

governance:
  - "PRN is the sole authority for identity-congruent behavioral rules."
  - "Directives must be injected into the LLM Bridge context."
  - "Historical directives are archived via the knowledge stream."