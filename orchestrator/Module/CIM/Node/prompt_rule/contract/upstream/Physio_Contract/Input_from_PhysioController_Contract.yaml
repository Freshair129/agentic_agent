# Input from PhysioController Contract
# Component: PhysioController -> PMT
# version: 8.1.0-R1

schema: EVA-Input-from-Physio-Contract-v1.0

required_inputs:
  physio_state:
    type: "object"
    description: "State vector for rule modulation."
    required: true
    properties:
      stress_level: { type: "float", range: [0, 1] }
      energy_level: { type: "float", range: [0, 1] }
