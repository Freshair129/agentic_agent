# Output to CIN Contract
# Component: MSP -> CIN
# version: 8.1.0-R1

schema: EVA-Output-to-CIN-Contract-v1.0

required_outputs:
  context_injection:
    type: "object"
    description: "Data provided to CIN for prompt building."
    required: true
    properties:
      turn_cache_summary: { type: "string" }
      rag_matches: { type: "array" }
      latest_matrix_state: { type: "object" }
