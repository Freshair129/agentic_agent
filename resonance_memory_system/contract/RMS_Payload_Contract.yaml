# ============================================================
# EVA MODULE CONTRACT: RMS Payload
# Version: 8.2.0-V2 (Strict)
# Status: LOCKED
# ============================================================

module: "ResonanceMemorySystem"
bus_channel: "bus:knowledge"
schema_ref: "file:///e:/The%20Human%20Algorithm/T2/EVA%208.2.0/resonance_memory_system/schema/RMS_Payload_Schema_v2.json"

payload_structure:
  episodic_snapshot:
    type: "Object"
    required: ["memory_encoding_level", "memory_color", "resonance_texture", "resonance_index", "trauma_flag"]
    description: "The final encoded memory snapshot."
  
governance:
  - "Every turn MUST publish to this contract."
  - "StateHash_S1 integrity must be verified before admission."
  - "Field types are immutable; additions require a V3 upgrade."