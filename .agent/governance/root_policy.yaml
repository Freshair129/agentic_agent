# EVA Root Governance Policy
# Version: 9.6.2
# Purpose: Enforce flat anatomy and prevent root pollution

# === ALLOWED ROOT FILES ===
# Updated: 2026-03-20 (cleanup completed)
allowed_root_files:
  - CLAUDE.md                # Claude Code context (MUST be at root)
  - .gitignore               # Git ignore patterns
  - .env.api                 # Environment config
  # REMOVED: *.code-workspace (deleted — contained external project refs)
  # REMOVED: LLM Structure Debate.md (moved to docs/10_References/)
  # REMOVED: GEMINI.md (moved to docs/10_References/)
  # REMOVED: CHANGELOG_SYSTEM.md (moved to docs/00_Governance/)
  # REMOVED: system_requirements.yaml (moved to .agent/standards/)
  # REMOVED: id_standards.yaml (moved to .agent/standards/)

# === ALLOWED ROOT DIRECTORIES ===
# These are auto-loaded from eva_master_registry.yaml:root_slots
allowed_root_dirs:
  - .git
  - .agent
  - .planning
  - .claude
  - registry
  - contracts
  - docs
  - operation_system
  - physio_core
  - eva_matrix
  - memory_n_soul_passport
  - orchestrator
  # - genesis_knowledge_system    # NOT YET IMPLEMENTED (directory does not exist)
  - resonance_memory_system
  - artifact_qualia
  - api
  - capabilities
  - consciousness
  # - data                        # Does not exist at root
  - memory
  - scripts
  - tests
  - tools

# === ENFORCEMENT SETTINGS ===
enforcement:
  pre_commit_hook: true       # Block commits with violations
  ci_check: true              # Run in CI pipeline
  developer_notify: warning   # Show warnings (not blocking errors)
  auto_cleanup: false         # Manual cleanup only

# === EXEMPTIONS (Temporary) ===
# All previous exemptions have been cleaned up (2026-03-20)
temporary_exemptions: []

# === SUGGESTED DESTINATIONS ===
# Where to move files that violate root policy
cleanup_destinations:
  "*.md":
    - docs/10_References/     # Technical references
    - docs/99_Archive/        # Old/deprecated docs
  "*.yaml":
    - .agent/tasks/           # Task definitions
    - .agent/workflows/       # Workflow definitions
  "*.rar":
    - docs/assets/            # Archived assets
